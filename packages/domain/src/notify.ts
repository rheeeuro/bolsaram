/**
 * 알림 정책 (마이그레이션 0017 의 아웃박스).
 *
 * 발송 코드는 웹 앱에, 정리 작업은 `@bolsaram/db` 에 있어서 서로를 임포트할 수 없다.
 * 두 곳이 같은 창을 봐야 하므로 숫자는 여기 하나만 둔다 — 재시도 창보다 정리가 먼저
 * 돌면 아직 보낼 수 있는 알림을 지우게 된다.
 */

/** 이보다 오래된 알림은 보내지 않는다. 늦게 도착한 알림은 알림이 아니라 소음이다. */
export const NOTIFICATION_RETRY_WINDOW_DAYS = 3;

/** 발송 시도 상한. 넘으면 더 집지 않는다 — 무한 재시도로 봇을 두드리지 않는다. */
export const NOTIFICATION_MAX_ATTEMPTS = 3;
